{
  "name": "sunrise",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  }
}
