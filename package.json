{
  "name": "sunrise",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "npm run dev --workspace=frontend",
    "backend": "dotnet run --project ./SunriseApi/SunriseApi.csproj",
    "build": "npm run build --workspace=frontend"
  }
}
